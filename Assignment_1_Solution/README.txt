* All the input nodes are in the test_nodes folder
* All the output files are in the outputs folder
* To run the program:

`ruby main.rb` in the console and then just give the node name you want to test as input.

*pre-requisite: The computer has to have ruby installed.
